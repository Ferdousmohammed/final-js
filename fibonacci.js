var fibo = [0, 1];
for(var i = 2; i<=12; i++){
    fibo[i]= fibo[i-1] + fibo[i-2];

    // console.log(fibo[i], fibo[i-1], fibo[i-2]);
}
console.log(fibo);

var vivo = [0, 1];
for(var i = 2; i<=12; i++){
    vivo[i]= vivo[i-1] + vivo[i-2];
}
console.log(vivo);

var eva = [0,1];
for(var i=2; i<=15; i++){
    eva[i]= eva[i-1]+ eva[i-2];
}
console.log(eva);